const SectionTitle = ({ children }: { children: React.ReactNode }) => {
  return (
    <h1 className="dark:text-zinc-400 font-bold uppercase text-xs">{children}</h1>
  )
}

export default SectionTitle
